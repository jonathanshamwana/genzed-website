import React from 'react';
import '../styles/Hero.css';

const Hero = () => {
    return (
        <div className="hero">
            <h1>GenZed</h1>
            <p className="hero-subtitle">
                Empowering Zambia's next generation of
            </p>
            <button className="join-btn">Join the Community</button>
        </div>
    );
};

export default Hero;